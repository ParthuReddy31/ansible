- name: Nginx install and run
  hosts: web
  become: yes
  tasks:
    - name: Installing Nginx
      ansible.builtin.package:
        name: nginx
        state: present
    - name: Starting Nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes
